{
  "name": "humanrank-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HumanRank backend — Hono attestor + x402 trial gate (port 8787)",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "start": "tsx --env-file-if-exists=.env src/index.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
